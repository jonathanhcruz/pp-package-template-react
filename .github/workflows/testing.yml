name: testing
on:
  pull_request:
    branches: [ main ]
jobs: 
  check-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v3
        with:
          node-version: 18.13.0
      - run: npm install 
      - run: npm run test
